<header class="navbar navbar-expand-md navbar-toggleable-md navbar-light bg-white border-bottom-light" style="background-color: #F7F7F7 !important">
    <div class="container-fluid">
        <div class="text-right">
            <%= link_to image_tag("logos/logo.png", width: '180'), root_path  %>
        </div>

        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarContent" aria-controls="navbarsExample07" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navbarContent">
            <% if !current_user.nil? %>
                <ul class="navbar-nav mr-auto mt-2 mt-md-0">
                    <li class="nav-item">
                        <a class="nav-link" href="<%= schools_path %>">Planteles</a>
                    </li>
                </ul>

                <ul class="navbar-nav ml-auto"> 
                    <%= link_to('Logout', destroy_user_session_path, method: :delete, :class => "nav-link") %>        
                </ul>
            <%end%>

            <% if !current_student.nil? %>
                <ul class="navbar-nav mr-auto mt-2 mt-md-0">
                    <li class="nav-item">
                        <a class="nav-link" href="<%= home_students_path %>">Cursos</a>
                    </li>

                    <li class="nav-item">
                        <a class="nav-link" href="<%= home_students_path %>">Dudas</a>
                    </li>

                    <li class="nav-item">
                        <a class="nav-link" href="<%= home_students_path %>">Comentarios</a>
                    </li>
                </ul>

                <ul class="navbar-nav ml-auto"> 
                    <%= link_to('Logout', destroy_student_session_path, method: :delete, :class => "nav-link") %>        
                </ul>
            <%end%>

            <% if !current_teacher.nil? %>
                <ul class="navbar-nav mr-auto mt-2 mt-md-0">
                    <li class="nav-item">
                        <a class="nav-link" href="<%= courses_path %>">Cursos</a>
                    </li>
                </ul>

                <ul class="navbar-nav ml-auto"> 
                    <%= link_to('Logout', destroy_teacher_session_path, method: :delete, :class => "nav-link") %>        
                </ul>
            <%end%>
            
            <% if !current_school.nil? %>
                <ul class="navbar-nav mr-auto mt-2 mt-md-0">
                    <li class="nav-item">
                        <a class="nav-link" href="<%= teachers_path %>">Maestros</a>
                    </li>

                    <li class="nav-item">
                        <a class="nav-link" href="<%= students_path %>">Estudiantes</a>
                    </li>

                    <li class="nav-item">
                        <a class="nav-link" href="<%= students_path %>">Comentarios</a>
                    </li>

                    <li class="nav-item">
                        <a class="nav-link" href="<%= students_path %>">Dudas</a>
                    </li>

                    <li class="nav-item">
                        <a class="nav-link" href="<%= students_path %>">Cuestionarios</a>
                    </li>

                    <li class="nav-item">
                        <a class="nav-link" href="<%= students_path %>">Registro planteles</a>
                    </li>
                </ul>

                <ul class="navbar-nav ml-auto"> 
                    <%= link_to('Logout', destroy_school_session_path, method: :delete, :class => "nav-link") %>        
                </ul>
            <%end%>
        </div>  
    </div>
</header>
<hr>