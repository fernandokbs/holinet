<div class="text-center">
    <% @course.videos.each do |video| %>
        <div class="mb-5 mt-5 embed-responsive embed-responsive-16by9">
            <%= video_tag  url_for(video), size: "250x120", controls: true %>
            <br>
        </div>
    <% end %>
</div>
